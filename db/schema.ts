import {sql} from "drizzle-orm";import {integer,real,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const partners=sqliteTable("partners",{id:integer("id").primaryKey({autoIncrement:true}),ownerEmail:text("owner_email").notNull().unique(),name:text("name").notNull(),phone:text("phone").notNull().default(""),website:text("website").notNull().default(""),description:text("description").notNull().default(""),rooms:integer("rooms").notNull().default(0),capacity:integer("capacity").notNull().default(0),imageKey:text("image_key"),status:text("status",{enum:["pending","active","paused"]}).notNull().default("pending"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const deals=sqliteTable("deals",{id:integer("id").primaryKey({autoIncrement:true}),dealId:text("deal_id").notNull().unique(),ownerEmail:text("owner_email").notNull(),venueName:text("venue_name").notNull(),title:text("title").notNull(),dealType:text("deal_type",{enum:["hot","last_minute","select","flex"]}).notNull(),visibility:text("visibility",{enum:["public","member","secret"]}).notNull(),regularPrice:real("regular_price").notNull(),dealPrice:real("deal_price").notNull(),minParticipants:integer("min_participants").notNull(),maxParticipants:integer("max_participants").notNull(),included:text("included").notNull().default(""),uniqueValue:text("unique_value").notNull(),validFrom:text("valid_from").notNull(),validTo:text("valid_to").notNull(),leadProtectionMonths:integer("lead_protection_months").notNull().default(6),status:text("status",{enum:["draft","active","paused"]}).notNull().default("draft"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const leads=sqliteTable("leads",{id:integer("id").primaryKey({autoIncrement:true}),leadId:text("lead_id").notNull().unique(),dealId:text("deal_id"),company:text("company").notNull(),contactName:text("contact_name").notNull(),email:text("email").notNull(),phone:text("phone").notNull().default(""),destination:text("destination").notNull(),meetingDate:text("meeting_date").notNull(),participants:integer("participants").notNull(),meetingType:text("meeting_type").notNull(),notes:text("notes").notNull().default(""),protectionMonths:integer("protection_months").notNull().default(6),protectionUntil:text("protection_until").notNull(),status:text("status",{enum:["new","matched","quoted","won","lost"]}).notNull().default("new"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});

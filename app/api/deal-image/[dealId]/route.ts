import {env} from "cloudflare:workers";import {and,eq} from "drizzle-orm";import {getDb} from "../../../../db";import {deals,partners} from "../../../../db/schema";
export async function GET(_:Request,{params}:{params:Promise<{dealId:string}>}){const{dealId}=await params,db=getDb(),[deal]=await db.select().from(deals).where(and(eq(deals.dealId,dealId),eq(deals.status,"active"))).limit(1);if(!deal)return new Response("Saknas",{status:404});const[profile]=await db.select().from(partners).where(eq(partners.ownerEmail,deal.ownerEmail)).limit(1);if(!profile?.imageKey)return new Response("",{status:404});const object=await env.BUCKET.get(profile.imageKey);if(!object)return new Response("",{status:404});const response=new Response(object.body);response.headers.set("content-type",object.httpMetadata?.contentType||"image/jpeg");response.headers.set("cache-control","public, max-age=300");return response}

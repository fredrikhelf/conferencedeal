"use client";
import {useState} from "react";import {ArrowRight,CheckCircle2} from "lucide-react";
export function LeadForm(){const[done,setDone]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const body=Object.fromEntries(new FormData(e.currentTarget).entries());try{const r=await fetch("/api/leads",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error);setDone(d.lead.leadId)}catch{setDone("MD-DEMO-"+Math.floor(10000+Math.random()*90000))}finally{setBusy(false)}}if(done)return <div className="lead-success"><CheckCircle2/><p className="kicker green">Förfrågan registrerad</p><h3>Tack. Nu börjar vi jämföra.</h3><p>Ert lead-ID är <strong>{done}</strong>. Spara det som referens.</p></div>;return <form className="lead-form" onSubmit={submit}><div className="form-grid"><label>Var vill ni mötas?<input required name="destination" placeholder="Stad, region eller restid"/></label><label>Önskat datum<input required type="date" name="meetingDate"/></label><label>Antal personer<input required type="number" min="8" name="participants" defaultValue="40"/></label><label>Typ av möte<select name="meetingType"><option>Helpension</option><option>Dagkonferens</option><option>Kickoff</option><option>Ledningsmöte</option><option>Företagsevent</option></select></label><label>Företag<input required name="company"/></label><label>Kontaktperson<input required name="contactName"/></label><label>E-post<input required type="email" name="email"/></label><label>Telefon<input name="phone"/></label></div><label>Vad är viktigast för er?<textarea name="notes" placeholder="Exempel: avskilt, bra mat, spa och max 2 timmar från Stockholm"/></label><button disabled={busy}>{busy?"Registrerar…":"Hitta min MeetingDeal"} <ArrowRight size={18}/></button><small>Kostnadsfritt och utan bokningstvång.</small></form>}

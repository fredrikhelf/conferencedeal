"use client";import {useState} from "react";import type {memberProfiles} from "../../db/schema";
export function MemberProfileForm({email,profile}:{email:string,profile:typeof memberProfiles.$inferSelect|undefined}){const[saved,setSaved]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");setSaved(false);const r=await fetch('/api/member-profile',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries()))}),d=await r.json();if(r.ok)setSaved(true);else setError(d.error||'Profilen kunde inte sparas.');setBusy(false)}return <form className="member-profile-form" onSubmit={submit}>{saved&&<div className="profile-saved">Företagsprofilen är sparad.</div>}{error&&<div className="data-error">{error}</div>}<section><h2>Företagsuppgifter</h2><div className="form-grid"><label>Företagsnamn<input required name="company" defaultValue={profile?.company||""}/></label><label>Kontaktperson<input name="contactName" defaultValue={profile?.contactName||""}/></label><label>E-post<input value={email} disabled/></label><label>Telefon<input name="phone" defaultValue={profile?.phone||""}/></label><label>Ort<input name="city" defaultValue={profile?.city||""}/></label><label>Antal anställda<input name="companySize" type="number" min="0" defaultValue={profile?.companySize||0}/></label></div><label>Vanliga mötesbehov<textarea name="meetingNeeds" defaultValue={profile?.meetingNeeds||""} placeholder="Exempel: ledningsmöten, kickoff och konferenser för 30–60 personer"/></label></section><button className="save-deal" disabled={busy}>{busy?'Sparar…':'Spara företagsprofil'}</button></form>}

"use client";import {useState} from "react";import type {deals,memberProfiles} from "../../db/schema";
export function MemberRequestForm({deal,profile,email}:{deal:typeof deals.$inferSelect,profile:typeof memberProfiles.$inferSelect|undefined,email:string}){const[done,setDone]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const body=Object.fromEntries(new FormData(e.currentTarget).entries()),r=await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,dealId:deal.dealId,destination:deal.venueName})}),d=await r.json();if(r.ok)setDone(d.lead.leadId);else setError(d.error||'Förfrågan kunde inte skickas.');setBusy(false)}if(done)return <div className="member-success"><h2>Förfrågan är skickad</h2><p>Ert referensnummer är <b>{done}</b>. MeetingDeal återkommer med nästa steg.</p></div>;return <form className="member-request-form" onSubmit={submit}>{error&&<div className="data-error">{error}</div>}<div className="form-grid"><label>Företag<input required name="company" defaultValue={profile?.company||""}/></label><label>Kontaktperson<input required name="contactName" defaultValue={profile?.contactName||""}/></label><label>E-post<input required type="email" name="email" defaultValue={email}/></label><label>Telefon<input name="phone" defaultValue={profile?.phone||""}/></label><label>Mötesdatum<input required type="date" name="meetingDate"/></label><label>Antal deltagare<input required type="number" min="8" name="participants" defaultValue={deal.minParticipants}/></label><label>Mötestyp<select name="meetingType"><option>Helpension</option><option>Dagkonferens</option><option>Kickoff</option><option>Ledningsmöte</option></select></label></div><label>Övriga önskemål<textarea name="notes" defaultValue={profile?.meetingNeeds||""}/></label><button className="save-deal" disabled={busy}>{busy?'Skickar…':'Skicka förfrågan'}</button></form>}

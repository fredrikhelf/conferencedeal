"use client";import {useState} from "react";import {CheckCircle2} from "lucide-react";
export function ProfileForm({email,initialName}:{email:string,initialName:string}){const[saved,setSaved]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch('/api/partner-profile',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok){setError(d.error||'Profilen kunde inte sparas.');return}setSaved(true)}return <form className="profile-form" onSubmit={submit}><div className="profile-progress"><div><b>Din profil är {initialName?"40":"25"}% komplett</b><p>Grunduppgifterna används på deals och i kontakten med kunder.</p></div><span>{initialName?"40%":"25%"}</span></div>{saved&&<div className="profile-saved"><CheckCircle2/>Profilen är sparad.</div>}{error&&<div className="data-error">{error}</div>}<section><h2>Grunduppgifter</h2><div className="form-grid"><label>Anläggningens namn<input required name="name" defaultValue={initialName}/></label><label>Kontaktmail<input value={email} disabled/></label><label>Telefon<input name="phone" placeholder="+46…"/></label><label>Webbplats<input name="website" placeholder="https://"/></label></div><label>Beskrivning<textarea name="description" placeholder="Beskriv anläggningen, miljön och vad som gör era möten speciella."/></label></section><section><h2>Kapacitet</h2><div className="form-grid"><label>Antal hotellrum<input name="rooms" type="number" min="0"/></label><label>Max möteskapacitet<input name="capacity" type="number" min="0"/></label></div></section><button className="save-deal">Spara profil</button></form>}

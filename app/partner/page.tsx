import Link from "next/link";import {desc,eq} from "drizzle-orm";import {ArrowRight,BarChart3,CalendarDays,Clock3,Eye,Handshake,Hotel,Plus,Tag} from "lucide-react";import {getDb} from "../../db";import {deals} from "../../db/schema";import {chatGPTSignOutPath,requireChatGPTUser} from "../chatgpt-auth";
export const dynamic="force-dynamic";
export default async function Partner(){const user=await requireChatGPTUser("/partner");let savedDeals:typeof deals.$inferSelect[]=[];let loadError=false;try{savedDeals=await getDb().select().from(deals).where(eq(deals.ownerEmail,user.email)).orderBy(desc(deals.createdAt)).limit(30)}catch{loadError=true}const active=savedDeals.filter(d=>d.status==="active").length;const drafts=savedDeals.filter(d=>d.status==="draft").length;return <main className="portal"><aside className="side"><Link className="brand inverse" href="/"><span>MEETING</span><b>DEAL</b></Link><div className="venue-switch"><small>PARTNERKONTO</small><b>{savedDeals[0]?.venueName||"Er anläggning"}</b><span>{user.email}</span></div><nav><a className="active" href="#"><BarChart3/>Översikt</a><a href="#my-deals"><Tag/>Mina deals</a><a href="#leads"><Handshake/>Leads</a><a href="#"><CalendarDays/>Kalender</a><a href="#"><Hotel/>Anläggningsprofil</a></nav><a className="signout" href={chatGPTSignOutPath("/")}>Logga ut</a></aside><section className="portal-main"><header><div><p className="kicker green">Partnerportal</p><h1>God eftermiddag, {user.displayName.split(" ")[0]}.</h1></div><Link className="portal-primary" href="/partner/deals/new"><Plus/>Skapa ny deal</Link></header>{loadError?<div className="data-error">Databasen kunde inte läsas just nu. Ladda om sidan och försök igen.</div>:savedDeals.length===0?<div className="notice"><Clock3/><div><b>Kom igång med er första MeetingDeal</b><p>Skapa ett unikt paket och välj de datum ni helst vill fylla.</p></div><Link href="/partner/deals/new">Skapa deal <ArrowRight/></Link></div>:<div className="notice saved"><Tag/><div><b>{savedDeals.length} {savedDeals.length===1?'deal är':'deals är'} sparade</b><p>{drafts} utkast och {active} aktiva deals.</p></div><Link href="/partner/deals/new">Skapa fler <ArrowRight/></Link></div>}<div className="metrics"><article><span><Eye/>Profilvisningar</span><b>0</b><small>Senaste 30 dagarna</small></article><article><span><Handshake/>Nya leads</span><b>0</b><small>Kopplade till era deals</small></article><article><span><Tag/>Sparade deals</span><b>{savedDeals.length}</b><small>{drafts} utkast · {active} aktiva</small></article><article><span><Clock3/>Svarstid</span><b>–</b><small>Mäts efter första leadet</small></article></div><div className="portal-grid"><section id="my-deals"><div className="panel-head"><h2>Mina deals</h2><Link href="/partner/deals/new">+ Ny deal</Link></div>{savedDeals.length?<div className="partner-deals">{savedDeals.map(d=><article key={d.id}><div><span className={`status ${d.status}`}>{d.status==="draft"?"Utkast":d.status}</span><small>{d.dealId}</small><h3>{d.title}</h3><p>{d.venueName} · {d.visibility}</p></div><div className="partner-price"><b>{d.dealPrice.toLocaleString("sv-SE")} kr</b><del>{d.regularPrice.toLocaleString("sv-SE")} kr</del><small>{d.validFrom} – {d.validTo}</small></div></article>)}</div>:<div className="portal-empty"><Tag/><h3>Inga deals sparade ännu</h3><p>När databasen bekräftar en deal visas den här direkt.</p></div>}</section><section><div className="panel-head"><h2>Profilstatus</h2><b>25%</b></div><div className="progress"><i style={{width:"25%"}}/></div><ul className="checklist"><li className="done">✓ Grunduppgifter</li><li>○ Bilder och beskrivning</li><li>○ Möteslokaler</li><li>○ Priser och villkor</li></ul></section></div></section></main>}

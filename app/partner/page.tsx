import Link from "next/link";import {desc,eq} from "drizzle-orm";import {ArrowRight,Clock3,Eye,Handshake,Plus,Tag} from "lucide-react";import {getDb} from "../../db";import {deals} from "../../db/schema";import {requireChatGPTUser} from "../chatgpt-auth";import {PortalNav} from "./PortalNav";
export const dynamic="force-dynamic";
export default async function Partner(){const user=await requireChatGPTUser("/partner");let rows:typeof deals.$inferSelect[]=[];let failed=false;try{rows=await getDb().select().from(deals).where(eq(deals.ownerEmail,user.email)).orderBy(desc(deals.createdAt)).limit(30)}catch{failed=true}const active=rows.filter(d=>d.status==="active").length,drafts=rows.filter(d=>d.status==="draft").length;return <main className="portal"><PortalNav active="overview" email={user.email} venue={rows[0]?.venueName}/><section className="portal-main"><header><div><p className="kicker green">Partnerportal</p><h1>God eftermiddag, {user.displayName.split(" ")[0]}.</h1></div><Link className="portal-primary" href="/partner/deals/new"><Plus/>Skapa ny deal</Link></header>{failed?<div className="data-error">Databasen kunde inte läsas just nu. Ladda om sidan.</div>:<div className={`notice ${rows.length?"saved":""}`}><Tag/><div><b>{rows.length?`${rows.length} sparade deals`:"Skapa er första MeetingDeal"}</b><p>{rows.length?`${drafts} utkast och ${active} aktiva deals.`:"Välj de datum ni helst vill fylla."}</p></div><Link href="/partner/deals/new">{rows.length?"Skapa fler":"Skapa deal"} <ArrowRight/></Link></div>}<div className="metrics"><article><span><Eye/>Profilvisningar</span><b>0</b><small>Senaste 30 dagarna</small></article><article><span><Handshake/>Nya leads</span><b>0</b><small>Kopplade till era deals</small></article><article><span><Tag/>Sparade deals</span><b>{rows.length}</b><small>{drafts} utkast · {active} aktiva</small></article><article><span><Clock3/>Svarstid</span><b>–</b><small>Mäts efter första leadet</small></article></div><div className="portal-grid"><section><div className="panel-head"><h2>Senaste deals</h2><Link href="/partner/deals">Visa alla</Link></div>{rows.length?<div className="partner-deals">{rows.slice(0,4).map(d=><article key={d.id}><div><span className={`status ${d.status}`}>{d.status==="draft"?"Utkast":d.status}</span><small>{d.dealId}</small><h3>{d.title}</h3><p>{d.venueName}</p></div><div className="partner-price"><b>{d.dealPrice.toLocaleString("sv-SE")} kr</b><small>{d.validFrom} – {d.validTo}</small></div></article>)}</div>:<div className="portal-empty"><Tag/><h3>Inga deals ännu</h3><p>Skapa en deal så visas den här.</p></div>}</section><section><div className="panel-head"><h2>Profilstatus</h2><b>25%</b></div><div className="progress"><i style={{width:"25%"}}/></div><ul className="checklist"><li className="done">✓ Grunduppgifter</li><li>○ Bilder och beskrivning</li><li>○ Möteslokaler</li><li>○ Priser och villkor</li></ul></section></div></section></main>}

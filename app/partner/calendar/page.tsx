import {asc,eq} from "drizzle-orm";import {CalendarDays} from "lucide-react";import {getDb} from "../../../db";import {deals} from "../../../db/schema";import {requireChatGPTUser} from "../../chatgpt-auth";import {PortalNav} from "../PortalNav";
export const dynamic="force-dynamic";
export default async function CalendarPage(){const user=await requireChatGPTUser("/partner/calendar");let rows:typeof deals.$inferSelect[]=[];try{rows=await getDb().select().from(deals).where(eq(deals.ownerEmail,user.email)).orderBy(asc(deals.validFrom))}catch{}return <main className="portal"><PortalNav active="calendar" email={user.email} venue={rows[0]?.venueName}/><section className="portal-main"><header><div><p className="kicker green">Partnerportal</p><h1>Kalender</h1></div></header><div className="calendar-legend"><span><i className="green-dot"/>Vill ha bokningar</span><span><i className="yellow-dot"/>Normal efterfrågan</span><span><i className="red-dot"/>Stängt</span></div><div className="content-panel"><div className="panel-head"><h2>Dealperioder</h2><b>{rows.length} registrerade</b></div>{rows.length?<div className="period-list">{rows.map(d=><article key={d.id}><div className="date-block"><b>{new Date(d.validFrom).getDate()}</b><small>{new Date(d.validFrom).toLocaleDateString("sv-SE",{month:"short"})}</small></div><div><span className={`status ${d.status}`}>{d.status==="draft"?"Utkast":d.status}</span><h3>{d.title}</h3><p>{d.validFrom} till {d.validTo} · {d.minParticipants}–{d.maxParticipants} personer</p></div></article>)}</div>:<div className="portal-empty"><CalendarDays/><h3>Inga datum registrerade</h3><p>Datum från era deals visas automatiskt i kalendern.</p></div>}</div></section></main>}

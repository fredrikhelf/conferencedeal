import Link from "next/link";import {desc,eq} from "drizzle-orm";import {Plus,Tag} from "lucide-react";import {getDb} from "../../../db";import {deals} from "../../../db/schema";import {requireChatGPTUser} from "../../chatgpt-auth";import {PortalNav} from "../PortalNav";import {DealActions} from "../../components/DealActions";
export const dynamic="force-dynamic";
export default async function DealsPage(){const user=await requireChatGPTUser("/partner/deals");let rows:typeof deals.$inferSelect[]=[];try{rows=await getDb().select().from(deals).where(eq(deals.ownerEmail,user.email)).orderBy(desc(deals.createdAt))}catch{}return <main className="portal"><PortalNav active="deals" email={user.email} venue={rows[0]?.venueName}/><section className="portal-main"><header><div><p className="kicker green">Partnerportal</p><h1>Mina deals</h1></div><Link className="portal-primary" href="/partner/deals/new"><Plus/>Skapa ny deal</Link></header><div className="content-panel">{rows.length?<div className="partner-deals large">{rows.map(d=><article key={d.id}><div><span className={`status ${d.status}`}>{d.status==="draft"?"Utkast":d.status==="active"?"Publicerad":"Pausad"}</span><small>{d.dealId} · {d.visibility}</small><h3>{d.title}</h3><p>{d.venueName} · {d.dealType.replace("_"," ")}</p></div><div className="partner-price"><b>{d.dealPrice.toLocaleString("sv-SE")} kr/person</b><del>Ord. {d.regularPrice.toLocaleString("sv-SE")} kr</del><small>{d.validFrom} – {d.validTo}</small><DealActions dealId={d.dealId} status={d.status}/></div></article>)}</div>:<div className="portal-empty"><Tag/><h3>Inga deals sparade</h3><p>Skapa er första deal för att börja fylla utvalda datum.</p><Link className="inline-action" href="/partner/deals/new">Skapa deal</Link></div>}</div></section></main>}

import Link from "next/link";import {ChevronLeft} from "lucide-react";import {requireChatGPTUser} from "../../../chatgpt-auth";import {DealBuilder} from "../../../components/DealBuilder";
export const dynamic="force-dynamic";
export default async function NewDeal(){const user=await requireChatGPTUser("/partner/deals/new");return <main className="builder-page"><header><Link href="/partner"><ChevronLeft/>Till översikten</Link><div className="brand"><span>MEETING</span><b>DEAL</b></div><small>{user.email}</small></header><div className="builder-wrap"><div className="builder-title"><p className="kicker green">Dealbuilder</p><h1>Skapa en deal som är värd resan.</h1><p>En stark MeetingDeal ska ge kunden en tydlig prisfördel eller ett konkret mervärde.</p></div><DealBuilder/></div></main>}

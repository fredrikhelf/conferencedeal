import {requireChatGPTUser} from "../../../chatgpt-auth";import {VenueForm} from "../../../components/VenueForm";import {PortalNav} from "../../PortalNav";export const dynamic="force-dynamic";export default async function NewVenue(){const user=await requireChatGPTUser('/partner/venues/new');return <main className="portal"><PortalNav active="venues" email={user.email}/><section className="portal-main"><header><div><p className="kicker green">Anläggningar</p><h1>Lägg till anläggning</h1></div></header><VenueForm/></section></main>}
